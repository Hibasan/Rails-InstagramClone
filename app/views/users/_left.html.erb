<div class="left_container">
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#gnavi">
          <span></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#"><%= @user.name %></a>
      </div>
      <div id="gnavi" class="collapse navbar-collapse">
        <ul class ="nav navbar-nav">
          <li><%= link_to "ホーム", user_path(@user.id) %></li>
          <li><%= link_to "お気に入り", favorite_path(@user.id) %></li>
          <li><%= link_to "全ユーザー投稿", blogs_path(id: @user.id) %></li>
          <% if main_user? %>
          <li><%= link_to "プロフィール変更", edit_user_path(current_user.id) %></li>
          <li><h2><%= link_to "新規投稿", new_blog_path(current_user.id) %></h2></li>
          <% end %>
        </ul>

      </div>
    </div>
  </nav>
</div>
